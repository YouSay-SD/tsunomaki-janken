import React from 'react';
import { Modal } from '../components';

export const ModalContainer = () => {
  return (
    <Modal>
      <Modal.Button>Share Result & leave a message</Modal.Button>
    </Modal>
  )
}
